// TEAM 1 - Take the carInfo and push out make, model and color in an array (or object of choice)

export default function parseCarInfo(carInfo: string):{make: string, color: string, model: string} {
  return {make: "Toyota", color: "red", "model": "Highlander"}
}